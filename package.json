{
  "private": true,
  "name": "manual-vite-react",
  "description": "Vite 툴체인을 사용해 React 개발 환경 매뉴얼 구성",
  "version": "0.0.1",
  "engines": {
    "node": "^16.0.0",
    "vscode": "^1.22.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.1.4"
  },
  "scripts": {
    "start": "npm run dev -- --open",
    "dev": "vite --host localhost --port 3000 --cors",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
